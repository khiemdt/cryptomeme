export interface FilterProps {
  id: number;
  name: string;
  grade: string;
  subjectNameId: number;
}
export interface MenuPracticeProps {
  name: string;
  type: string;
  url: string;
  subMenu?: Array<any>;
}
